# App Suite

Minimal clean Flutter monorepo scaffold.

- `apps/` contains `user_app`, `partner_app`, and `admin_app`
- each app is a plain hello-world starter
- `packages/` is kept as a clean placeholder for future shared code

Add real features later without changing the folder layout.